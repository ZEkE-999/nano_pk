# nano_pk
A lightweight Dockerized Python bridge that connects an Nano-PK pellet boiler via Telnet to MQTT and Home Assistant.  This project reads live boiler data, converts it into structured MQTT topics, and enables full Home Assistant auto-discovery.
